#include <stdio.h>

/**
 * a program that copies it input to its output,
 * replacing each string of one or more blanks
 * by a single blank
 */

int main(void)
{
    int c, nl, bl, tb;

    nl = 0;
    while ((c = getchar()) != EOF)
    {

    putchar(c);
    } 
    
    return (0);
}
